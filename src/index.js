/**
 * AI Council for Claude Code
 *
 * Multi-agent consensus system for better architectural decisions
 */

const { install, uninstall, addAgent } = require('./installer');

module.exports = {
  install,
  uninstall,
  addAgent
};
